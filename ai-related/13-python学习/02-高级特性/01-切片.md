取一个list或tuple的部分元素是非常常见的操作。

```plain
>>> L = ['Michael', 'Sarah', 'Tracy', 'Bob', 'Jack']
>>> 取前三个
>>> 笨蛋方法：
[L[0], L[1], L[2]]
['Michael', 'Sarah', 'Tracy']

或者
r = []
n = 3
for i in range(n):
r.append(L[i])

使用切片：
L[0:3]
第一个是0；可以省略为：L[:3]
也可以取倒数：L[-5:-3]

```


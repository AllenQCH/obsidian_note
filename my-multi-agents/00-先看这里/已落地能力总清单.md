---
title: "已落地能力总清单"
source: "conversation: Codex chat 2026-06-07"
author: "Codex"
published:
created: 2026-06-07
description: "把当前个人 Codex 多 agent 改造已经落地的运行态、历史参考、文档入口和下一步动作压缩成一篇总清单。"
tags: ["codex", "agent", "workflow", "registry", "overview"]
type: "workflow"
status: "processed"
---

# 已落地能力总清单

## 摘要

这篇笔记只解决一个问题：

> 现在这轮个人 Codex 多 agent 改造，实际已经做完了什么？后面如果不想翻很多篇文档，最少只需要记住什么？

先说结论：

- 你的本地 `~/.codex` 四层体系已经落地成运行态
- active / draft 的边界已经明确
- 常用阅读入口已经整理好
- `tool_dbauto_sql_operator` 已经正式进入运行态
- `tool_dbauto_sql_operator` 已完成第一轮本机只读可用性验证
- `tool_trace_log_operator` 已经正式进入运行态
- `tool_trace_log_operator` 的历史设计线与复盘资料也已经保留完整
- `tool_xxljob_execute_once_operator` 已进入运行态，用于生产 XXL-Job `jobId=280` 的受限 `执行一次` 操作
- `tool_weekly_report_operator` 已进入运行态，用于只读查询蓝鲸周报信息并按固定格式输出本周、下周、后三周内容
- `tool_gmail_classifier_operator` 已进入运行态，用于 Gmail Inbox label 归类，并由本机 launchd 每周三 09:30 自动触发
- `tool_auth_permission_seed_operator` 已进入运行态，用于 HSP 权限定义 SQL 生成、校验 SQL、接口权限路径核对和无权限链路排查
- `tool_cls_log_query_operator` 已进入运行态，用于国内腾讯云 CLS 日志查询，支持 trace/message/CLS 链接输入，并按 service -> manager -> center 链路提取第一异常点
- `tool_intl_pof_sh_confirm_operator` 已进入运行态，用于海外 POF 收货单满数量确认前的 dbauto 导出核查、curl 组装、校验 SQL 和风险拦截
- `tool_bk_pipeline_operator` 已进入运行态，用于蓝鲸需求流水线和服务子流水线操作；迭代再开发 push 后默认复用原需求流水线，只重跑本次改动影响的已有服务子流水线
- `stage_test_runner` 已进入运行态，用于代码改动后的测试设计、测试执行和 pass / fail 判定
- `tool_github_web_operator` 已进入运行态，用于 GitHub 网页设置和浏览器确认类动作
- `tool_github_web_operator` 已接入 `github-web-operations` Skill：默认复用当前 Chrome 登录态和稳定 `github-web` session；`gh auth status` 只是能力检查，失败不得自动要求 CLI 授权，OpenCLI 也不提取或转换浏览器 token
- `tool_personal_migration_curator` 已进入运行态，用于个人 agent / skill 可迁移资源整理
- `tool_invoice_application_full_flow_delete_operator` 已进入运行态，用于发票申请单全链路删除包生成

也就是说，现在已经不是“还在讨论怎么拆”的阶段，而是：

> 主干已经稳定，后面该做的是继续拿真实任务验证和扩能力。

补充一个你后面读文档时要统一的中文叫法：

- `new_feature_from_scratch` 对外统一叫 `新项目开发`
- `iterative_feature_development` 对外统一叫 `迭代开发`
- `existing_feature_continuation` 对外统一叫 `迭代再开发`

## 核心内容

### 1. 当前运行态已经落地的层级

当前主配置真源仍然是：

- [config.toml](/Users/heytea/.codex/config.toml:1)

当前已经 active 的四层结构：

```text
control
stage
tool
gate
```

#### Control

- `control_request_router`
- `control_stage_orchestrator`

#### Stage

- `stage_task_planner`
- `stage_investigation_planner`
- `stage_execution_runner`
- `stage_test_runner`
- `stage_integration_orchestrator`
- `stage_closeout_reporter`

#### Tool

- `tool_sso_operator`
- `tool_dbauto_operator`
- `tool_dbauto_sql_operator`
- `tool_excel_operator`
- `tool_obsidian_operator`
- `tool_github_sync_operator`
- `tool_github_web_operator`
- `tool_trace_log_operator`
- `tool_cls_log_query_operator`
- `tool_xxljob_execute_once_operator`
- `tool_weekly_report_operator`
- `tool_gmail_classifier_operator`
- `tool_personal_migration_curator`
- `tool_invoice_application_full_flow_delete_operator`
- `tool_auth_permission_seed_operator`
- `tool_intl_pof_sh_confirm_operator`
- `tool_bk_pipeline_operator`
#### Gate

- `gate_stage_evaluator`

### 2. 当前本地文件结构已经稳定

`~/.codex/agents/` 现在已经按层级整理：

- `control/`
- `stage/`
- `operator/`
- `gate/`
- `docs/`

其中最重要的两个入口是：

- [~/.codex/agents/README.md](/Users/heytea/.codex/agents/README.md:1)
- [~/.codex/agents/docs/README.md](/Users/heytea/.codex/agents/docs/README.md:1)

如果你以后想先看“运行态文档入口”，先开这两个就够了。

### 3. 当前最关键的判断规则

最重要的一条规则现在已经收敛清楚了：

> 只有写进 `~/.codex/config.toml` 的 agent，才算 active。

这条规则意味着：

- 有 Obsidian 草案，不算 active
- 有 `.draft.toml`，也不算 active
- 有 workflow / contract 草案，还是不算 active
- 有 harness markdown、项目流程文档、OpenSpec 或 prompt 模板，也不算“已实现 agent”
- 用户要求“实现 agent / 新增 agent / 做成 agent”时，默认必须落成 `~/.codex/agents/<layer>/*.toml` 并注册进 `~/.codex/config.toml`

active / draft 的最快判断入口：

- [[哪些agent已启用哪些还只是草稿]]

### 4. 现在最值得保留的阅读入口

如果你后面想快速找回这套体系，优先看这几篇：

#### 总入口

- [[最新multi-agent流程总览]]
- [[阅读顺序-先Xuetao再我的agents]]
- [[已落地能力总清单]]
- [[当前阶段和下一步]]

#### 本地运行态

- [[我的Codex多agent改造计划]]
- [[本机已启用agent注册表]]
- [[我的.codex目录里有什么]]

#### 实操样板

- [[Obsidian写入和GitHub同步怎么跑]]
- [[问题排查先查什么证据]]

### 5. dbauto 现在已经拆成两种能力

这次补齐以后，`dbauto` 相关能力已经不是一个模糊大块，而是明确拆成了两类：

- `tool_dbauto_operator`
  - 负责 export runtime bootstrap
- `tool_dbauto_sql_operator`
  - 负责国内 dbauto 只读 SQL / 列库表 / 元数据查询

对应样板：


当前验证事实：

- `opencli doctor -v` 已通过
- `dbauto_sql_query.py --help` 已通过
- `dbauto_sql_query.py --list-common-instances` 已真实返回 3 个常用实例
- `dbauto_sql_query.py --instance hsp-ids --list-dbs` 已真实返回 3 个数据库
- `dbauto_sql_query.py --instance hsp-ids --db center_hsp_invoice --list-tables` 已真实返回 invoice 表清单
- `SHOW CREATE TABLE hsp_goods_rate` 已成功
- `SHOW COLUMNS FROM hsp_goods_rate` 已成功
- `DESC hsp_goods_rate` 当前不受支持，应优先使用 `SHOW` 系列元数据语法

### 6. 周报查询这条线现在到什么程度

`tool_weekly_report_operator` 当前已经是：

- active tool agent

它的默认用途是：

- 用户说“把周报信息查给我”“整理本周周报内容”等周报相关请求时；
- 只读查询蓝鲸 `智慧门店/b1af00` 的 TASK；
- 按 `relation=CREATED`、不限制状态、预计开始/结束时间都落入目标周的规则过滤；
- 本周工作读取工时插件 `jobContent`；
- 下周工作读取下周任务，无任务且上下文确认请假时输出 `请假`；
- 后三周需求读取关联需求并去重输出。

边界：

- 不提交工时；
- 不创建蓝鲸任务；
- 不修改任务状态；
- 不填写 AliDocs 表格。

### 7. Gmail 归类这条线现在到什么程度

`tool_gmail_classifier_operator` 当前已经是：

- active tool agent

它的默认用途是：

- 按既有 Gmail label 体系归类 `allenqi568@gmail.com` 的 Inbox 邮件；
- 已知来源归入工作机会、金融账单、安全账号、社交社区、工具产品通知、学习资讯、购物旅行等 label；
- 未知个人发件人继续留在 Inbox；
- 每周三 09:30 由本机 launchd 自动触发；
- 每次运行写入 `~/.codex/logs/gmail-classifier/` 的 JSON 报告。

边界：

- 默认 dry-run；
- launchd 才使用 `--execute`；
- 无人值守执行依赖 `~/.hermes/google_token.json`；
- OAuth token 缺失时只报告 `auth_required=true`，不会尝试浏览器 UI 自动化；
- 不删除邮件，不把未知个人邮件自动丢进 `99_低优先归档`。

### 8. trace-log 这条线现在到什么程度

`tool_trace_log_operator` 当前已经是：

- active tool agent

但它已经不再是零散想法，而是有一整条专题线：

- [[trace日志agent怎么拆]]
- [[trace日志agent输入输出约定]]
- [[trace日志最小执行入口]]
- [[哪些agent已启用哪些还只是草稿]]
- [[trace日志agent什么时候能启用]]
- [[trace日志真实案例模板]]
- [[trace日志案例填写示例]]
- [[trace日志agent晋升评审]]
- [[trace日志真实任务怎么记录]]
- [[trace日志agent是做什么的]]

如果只记一句：

> trace-log 这条线现在已经正式进入运行态，同时也保留了完整的“怎么开始、怎么记录、怎么填写、怎么复盘”的辅助文档。

### 9. 现在真正还没做的是什么

这轮不是没做完，而是已经从“架构整理”推进到“等待真实任务验证”的阶段。

当前还没做的是：

1. 还没有用更多真实 traceId 任务去积累稳定案例
2. 还没有完成一轮“active 后”的真实使用复盘
3. 还没有继续选择“下一批之后”的新高频 skill

这 3 个都属于下一阶段，不属于当前整理阶段遗漏。
### 10. 后面你最省事的使用方式

如果你后面不想再翻很多文档，可以这样走：

#### 想回忆整体架构

看：

- [[已落地能力总清单]]
- [[本机已启用agent注册表]]

#### 想看当前运行态

看：

- [config.toml](/Users/heytea/.codex/config.toml:1)
- [[哪些agent已启用哪些还只是草稿]]
- [[本机已启用agent注册表]]

#### 想确认提测文档落点

默认规则已经收敛为：

- 国内需求：在线提测文档创建到《国内迭代》
- 海外需求：在线提测文档创建到《海外迭代》
- 如果目标目录无法解析，流程应该阻断并要求补充 folderId，不允许创建到通用空间根目录

当前 `alidocs-test-delivery-doc` 已支持 `region=cn` 自动解析到《国内迭代》。

#### 想看 trace-log 这条线怎么使用和复盘

看：

- [[trace日志agent是做什么的]]

#### 真遇到 traceId 排查任务

直接从：

- [[trace日志最小执行入口]]

开始，然后再配合：

- [[trace日志案例填写示例]]
- [[trace日志真实案例模板]]

#### 真遇到国内 dbauto 查库表或只读 SQL

直接看：


### 10. 新的开发测试门禁

当前 multi-agent 运行态新增了一条开发收口规则：

> 生产代码开发任务，不能只靠编译或单测通过就宣布结束。

现在默认要求两层验证都过：

1. 代码级验证
   - 编译 / 构建通过
   - 单测通过
   - 仓库基线中的上下文测试通过，或明确记录为环境债务
2. 轻量本地联调验证
   - 只起最小本地环境
   - 只建最小表
   - 只造最小 mock 数据
   - 屏蔽 Apollo / Eureka / 非必要外部依赖
   - 只验证本次需求改动链路

对以下类型改动，第二层验证不再是可选项，而是默认必做项：

- 数据落表
- 定时任务
- 审核校验
- 跨服务接口编排
- 页面维护数据的新增 / 编辑 / 导出

这条规则已经同步进：

- `~/.codex/agents/docs/agent-workflows.md`
- `~/.codex/agents/docs/stage-gates.md`
- `~/.codex/agents/docs/agent-contracts.md`
- `~/.codex/agents/docs/active-agent-quickstart.md`

### 11. 通用本地闭环测试规范

当前又补齐了一条更具体的测试 agent 规则：

> 当需求涉及真实业务数据路径时，测试 agent 不能只生成单测或只验证服务启动；必须从真实入口触发，尽量跑真实内部链路，只 mock 外部依赖，并用本地业务结果证明改动生效。

补充约束：

- 定时任务、异步任务、消费者和数据库写入链路不能只用单测、mapper 测试或 service 层集成测试替代本地闭环。
- 必须启动参与链路的本地服务，触发真实 handler/controller/consumer/local runner，并通过本地数据库、下游 API、消息状态或生成物验证业务结果。
- 如果服务启动失败，必须记录启动命令、第一条阻断错误、所需中间件状态和下一步动作；这种情况只能标记为本地闭环阻断，不能写成完整测试通过。

这条规则已经沉淀为：

- `~/.codex/agents/docs/local-closure-testing-workflow.md`

适用场景包括：

- 数据库写入、更新、状态流转
- 定时任务、异步任务、MQ consumer、回调、重试
- 跨服务链路，例如 BFF -> manager -> center -> database
- 审核、审批、幂等、补偿流程
- 管理页查询、编辑、导出

本地闭环辅助代码允许入仓，但必须满足：

- 集中在 `config/localtest` 或等价本地测试目录
- 只在 `local-*` profile 或显式开关下生效
- 不影响默认 profile、测试环境、预发环境和生产环境
- OpenSpec 或需求记录必须写清楚本地资产、启用条件、执行命令、验证证据和环境边界
- 如果满足以上条件，并且该资产已经真实用于闭环验证，则默认纳入提交候选；后续统一按“正式需求代码 + 测试 + OpenSpec + 已隔离本地闭环资产”的方案处理

### 12. 新增的多仓库开发执行顺序

对多仓库开发需求，当前又补齐了一条更实用的执行顺序：

1. 先完成路由、OpenSpec 和文件级 diff plan。
2. 迭代开发默认不再逐仓库确认来源分支和目标需求分支名。
3. 对每个实际改动仓库先 `fetch origin`，切回 `master`，`pull --ff-only origin master`。
4. 再基于最新 `master`，按既定命名规则创建规范需求分支。
5. 分支创建完成后，先在每个实际改动仓库里补服务级 OpenSpec，再开始编码实现。

只有用户指定非 `master` 来源分支、分支名无法按需求编码/需求名生成、涉及仓库不明确、远端同名分支存在新建/复用歧义，或本地/远端分支状态冲突时，才回到人工确认。

### 13. 分层映射边界规则

当前又固化了一条更具体的多层后端分工规则：

> 当问题只是展示字段重命名、DTO 别名兼容或前端响应整形时，不要默认把改动下推到 `center` 或 mapper 层；优先在 `manager`、`backend`、`convert` 这类上层编排/展示层处理。

默认下推到 `center` 的前提应当是：

- SQL 过滤条件缺失
- 返回列缺失
- 持久化契约错误
- 下层领域对象本身不完整

如果只是 `salesInfo -> sellerInfo` 这类展示属性改名，则默认不在 `center` mapper 层改。

这条顺序适用于多服务的 `新项目开发` 和 `迭代开发`，可以减少“分支基线不一致”以及“OpenSpec 只留在组级目录、没落到代码仓库”的问题。

### 14. 人工控制简化规则

当前 multi-agent 流程已经把人工审核点从“每一步都确认”收敛为“按风险暂停”：

- 自动推进：只读分析、本地文件读取、本地 OpenSpec / 需求记录更新、确定性路由摘要、迭代开发默认从最新 `master` 创建规范需求分支、本地实现与验证、需求/设计确认且测试通过后的 commit/push、用户明确要求提测后的 BK 需求流水线创建与在线提测文档创建。
- 一次性授权：迭代开发默认把路由、需求/设计方向、允许的外部写动作合并成前置人工控制区；确认后，不再把拉 `master`、创建规范分支、写代码、写测试、跑测试、测试通过后的 commit/push 拆成重复人工确认。默认路由交付是输出单行启动命令和完整 prompt，由用户自行开新窗口粘贴，不作为额外人工确认点。
- 测试失败处理：agent 先自动定位失败原因；只有修复方案会改变已确认设计、扩大范围、改变接口契约，或需要接受失败继续推进时，才回到人工确认。
- 必须暂停：路由或工作区歧义、来源/目标分支缺失、破坏性 Git、强推、删除分支、合并主干、审批放行、环境晋级、停止/重试生产部署、在线文档删除/移动、同名流水线新建还是复用不明确。

这条规则已经沉淀到：

- `~/.codex/agents/docs/human-control-policy.md`
- `~/.codex/agents/docs/agent-workflows.md`
- `~/.codex/agents/docs/stage-gates.md`
- `/Users/heytea/Documents/myHeytea/codex-workspace/AGENTS.md`

### 15. 迭代再开发自主闭环规则

`existing_feature_continuation` 的重点是前期沟通，而不是重新跑完整开发交付。

默认流程：

1. 路由确认为 `existing_feature_continuation`。
2. 读取已有 OpenSpec、分支、历史提交、历史测试、提测文档和流水线记录；如果本次会触达交付文档或流水线，必须先恢复原在线 AliDocs 提测文档 URL/nodeId、BK 主需求流水线和受影响服务子流水线映射。
3. 人工确认本次续改目标，以及是否改变原设计。
4. 确认后自动修改代码、生成或更新测试规则、执行代码级验证和必要的本地闭环验证。
5. 测试通过后自动 commit/push。
6. 默认不新建 BK 流水线；push 后复用原需求流水线，按本次实际改动服务重跑已有服务子流水线。如果新增服务、仓库、部署单元或服务分支条目，优先把新增服务补进原有需求流水线。
7. 如果改动依赖包，先 deploy/publish 依赖包，再重跑原需求下所有已有服务子流水线；依赖包按公司项目服务名前缀先粗判：`api`、`service`、`manager`、`center` 开头默认不是依赖包，非这些前缀是依赖包候选。候选已在项目 `AGENTS.md` 登记时可直接处理；不认识时先找用户确认，并把确认结果写入项目 `AGENTS.md`。只确认目标子流水线 RUNNING 且无即时任务报错，不默认等终态。
8. 只有提测文档中的涉及服务、设计、接口、前端对接、外部对接、测试范围、风险说明或验证证据发生变化时，才更新原提测文档。
9. 在线提测文档按 6 个 sheet 定位更新：`服务清单` 写服务与流水线，`提测配置清单-TEST` 写测试环境 Apollo/配置，`提测配置清单-PROD` 写生产 Apollo/配置，`数据库脚本` 写 DDL/DML，`定时任务 XXL-JOB` 写定时任务，`发布流程` 写发布步骤。

补充：依赖包识别与 deploy/publish 规则不只适用于迭代再开发，也适用于 `new_feature_from_scratch` 和 `iterative_feature_development`。其中迭代开发在 commit/push 后，如果用户已要求提测、部署、打开流水线或 release handoff，先处理已确认的依赖包 deploy/publish，再触发受影响下游服务流水线；未知依赖包候选仍先确认并写回项目 `AGENTS.md`。

需要人工确认的情况：

- 不清楚复用哪个 OpenSpec、分支、提测文档或流水线记录。
- 本次续改目标可能改变原设计。
- 需要新建分支而不是复用旧分支。
- 原流水线无法定位，或新增服务的名称、分支、项目、模板、部署语义不明确。
- 测试失败后的修复方案会改变设计、扩大范围、改变接口契约，或需要接受失败继续推进。
- 用户明确要求重新提测、创建新流水线或新 release handoff。

### 16. codex-downloads 统一 OpenSpec 归档层

当前新增了一个跨项目需求总索引：

- `/Users/heytea/Documents/myHeytea/code/codex-downloads/openspec/`

用途：

- 一眼查看最近做过的需求。
- 汇总涉及仓库、服务、分支、commit、测试、流水线、提测文档、前端对接和外部对接。
- 归档不属于单一服务仓库的交付材料。
- 给 `新项目开发`、`迭代开发`、`迭代再开发` 和提测交付提供统一导航入口。

目录规则：

- `active/`：进行中的需求索引。
- `archived/<year>/`：已完成需求索引。
- `attachments/<需求编号>/`：前端对接文档、服务对接文档、提测文档、BK pipeline JSON、AliDocs evidence JSON 等附件。
- `templates/demand-index-template.md`：统一索引模板。

边界：

- 统一 OpenSpec 不替代服务级 OpenSpec。
- 服务级 OpenSpec 仍放在实际服务仓库，随服务代码 commit/push。
- 统一 OpenSpec 只做跨项目导航、交付材料归档和最近工作总览。
- 不写入密钥、cookie、token、生产数据导出或敏感个人信息。

## 可执行动作

1. 以后如果只想快速回忆这轮改造结果，优先先看这篇。
2. 下一阶段如果继续推进，实现方向优先二选一：
   - 用真实 trace 排查任务验证 `tool_trace_log_operator`
   - 继续选“下一批之后”的高频 skill 正式落成新的 `tool_*`
3. 在新的真实验证开始前，不需要再继续补很多总览型文档了。

## 相关链接

- [[阅读顺序-先Xuetao再我的agents]]
- [[我的Codex多agent改造计划]]
- [[本机已启用agent注册表]]
- [[哪些agent已启用哪些还只是草稿]]
- [[trace日志agent是做什么的]]
- [[trace日志真实任务怎么记录]]
- [[trace日志最小执行入口]]
- [[trace日志案例填写示例]]
- [[trace日志agent晋升评审]]
- [[问题排查先查什么证据]]
- [[当前阶段和下一步]]

## 变更记录

- 2026-06-08：补充一条路由输出约定。开发需求在杂事工作区首轮路由时，默认只给“已确认事实、路由判断、启动命令”，先让用户确认对不对；详细 prompt 只在确认后再展开。
- 2026-06-08：继续收敛路由交付格式。默认直接给“路由确认摘要 + 单行 `codex --cd ...` 启动命令 + 一整段可直接粘贴的 prompt”，并把已读取的需求文档结论直接带进 prompt，避免主项目重复读同一文档。
- 2026-06-08：自测后再收紧一条约束。首轮路由回复禁止再写“如果你确认，我再生成 prompt/命令”这类二次等待句式；命令和完整 prompt 必须在首轮一起给出。
- 2026-06-08：继续提高交接 prompt 质量。首轮 prompt 必须基于这次真实分析结果生成，优先写清路由依据、关键改造点和主项目下一步要先分析什么，不再满足于“格式正确但内容模板化”。
- 2026-06-08：继续把首轮路由交付结构写死。默认固定为 4 段：已确认事实、路由判断、启动命令、可直接粘贴的 prompt；避免把关键信息散落在大段说明里。
- 2026-06-10：补充通用本地闭环测试规范。新增 `local-closure-testing-workflow.md`，并同步 workflow、gate、contract、quickstart 和 Obsidian 总入口；测试 agent 后续应在生成测试用例后按该规范执行真实入口到本地业务结果的闭环验证。
- 2026-06-10：补充默认提交候选口径。以后若本地闭环资产已隔离、可复用、已写入 OpenSpec 且真实参与验证，则默认按方案 B 纳入提交候选，不再默认剔除。
- 2026-06-10：补充分层映射边界规则。以后遇到展示字段重命名或 DTO 别名兼容，默认保持在 `manager` / `backend` / `convert` 层处理，不默认下推到 `center` mapper 层。
- 2026-06-11：补充人工控制简化规则。multi-agent 流程改为“按风险暂停 + 一次性授权包”：确定性、本地、只读或已显式请求的动作自动推进；分支歧义、破坏性 Git、主干合并、审批/环境晋级、在线文档删除移动、同名流水线新建/复用歧义仍必须人工确认。
- 2026-06-11：补充迭代开发自主闭环口径。路由、需求/设计作为前置人工控制点；默认由用户自行开新窗口粘贴启动命令和 prompt，不单独确认“是否启动独立窗口”。确认后 agent 自动拉最新 `master`、创建规范分支、实现、写测试、跑测试、测试通过后 commit/push，并在已要求提测时继续创建 BK 流水线和在线提测文档。测试失败时先自动定位，只有改造方案变化或需要带失败推进时才回到人工确认。
- 2026-06-11：补充迭代再开发自主闭环口径。`existing_feature_continuation` 默认前期确认续改目标和设计差异；确认后自动编码、测试、验证、commit/push。默认不新建 BK 流水线；如果新增服务则补进原有流水线；只有服务或交付内容变化时更新原提测文档。
- 2026-07-02：补充迭代再开发 push 后流水线重跑口径。继续开发不新建主流水线或服务流水线，但需要复用原需求流水线，只重跑本次改动影响的已有服务子流水线；依赖包改动必须先 deploy/publish，再重跑原需求下所有已有服务子流水线。
- 2026-07-05：补充公司项目依赖包识别规则。`api`、`service`、`manager`、`center` 开头默认视为具体服务；非这些前缀为依赖包候选。候选若已在项目 `AGENTS.md` 登记则可直接走 deploy/publish；未登记时先向用户确认，并把确认结果写回项目 `AGENTS.md`，供后续自动判断。
- 2026-07-05：补齐 `iterative_feature_development` 的依赖包收口。迭代开发和新项目开发、迭代再开发一样适用公司项目依赖包识别规则；改动已确认依赖包时，commit/push 后先 deploy/publish，再触发受影响下游服务流水线。
- 2026-07-05：加固 testing role 本地闭环门禁。定时任务、异步任务、消费者和数据库写入链路必须启动参与服务并触发真实入口，验证本地业务结果；如果服务启动失败，只能记录为本地闭环阻断，不能用单测结果替代完整测试通过。
- 2026-06-11：新增 `codex-downloads/openspec` 统一归档层。后续跨仓库需求、前端/外部对接、流水线证据、提测文档和最近工作总览统一在该目录建立索引；服务级 OpenSpec 仍保留在各服务仓库内。
